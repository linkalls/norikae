# Copy to .env and fill in as needed

PORT=3000

# OAuth access token for auth-required endpoints (route memo, my timetable)
# Leave blank for public APIs (search, suggest, diainfo, timetable)
NORIKAE_ACCESS_TOKEN=your_oauth_token_here
